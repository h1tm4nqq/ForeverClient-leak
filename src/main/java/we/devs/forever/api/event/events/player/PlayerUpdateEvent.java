package we.devs.forever.api.event.events.player;

import we.devs.forever.api.event.EventStage;

public class PlayerUpdateEvent extends EventStage {
    public PlayerUpdateEvent() {
        super();
    }
}
